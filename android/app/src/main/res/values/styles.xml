<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!-- The launch theme parented Theme.SplashScreen but only ever set a plain
         background drawable, so on Android 12+ the system splash fell back to
         the launcher icon and nothing animated. These are androidx
         core-splashscreen's compat attributes (no android: prefix), so they
         apply on every API level the library backports to. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/imani_mark_animated</item>
        <!-- Must cover the animation or the system cuts it off mid-morph.
             Android caps this at 1000ms. -->
        <item name="windowSplashScreenAnimationDuration">600</item>
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>