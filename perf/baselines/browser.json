{
  "cold-boot": {
    "ms": 108,
    "tolerancePercent": 35,
    "ceilingMs": 1000,
    "note": "Measured on a laptop against the built bundle, logged out. The 1000ms ceiling is absolute: it is roughly the point at which opening the wallet stops feeling immediate, and it exists so that a baseline creeping upward a few percent at a time cannot ratchet past what a customer will tolerate while every run still reports green. Raising it is a product decision, not a maintenance one. The 35% band is deliberately wider than it looks like it needs to be: at 25% this run reported a regression at 138ms purely because a browser verification was running on the same machine, and was back to 108ms when idle. A band that cries wolf on a busy laptop is a band people learn to ignore, and the absolute ceiling is what actually guards the number."
  }
}
